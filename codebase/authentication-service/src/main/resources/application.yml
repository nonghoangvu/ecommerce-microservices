server:
  port: 8081
spring:
  application:
    name: authentication-service
  profiles:
    active: @spring.profiles.active@

management: # Health check
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    root: INFO
    web: OFF
