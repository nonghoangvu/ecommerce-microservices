spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
openapi:
  service:
    api-docs: auth-service
    server: ${HOST_NAME:http://localhost}:4953/auth
    title: Authentication service
    version: 1.0.0