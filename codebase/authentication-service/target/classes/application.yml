server:
  port: 8081
spring:
  application:
    name: authentication-service
  profiles:
    active: dev

management: # Health check
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    root: INFO
    web: OFF
