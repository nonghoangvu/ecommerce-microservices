server:
  port: 8082
spring:
  application:
    name: account-service
  profiles:
    active: dev

management: # Health check
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    root: INFO
    web: OFF
