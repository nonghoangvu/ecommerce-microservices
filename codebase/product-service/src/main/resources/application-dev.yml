spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
openapi:
  service:
    api-docs: product-service
    server: ${HOST_NAME:http://localhost}:8083
    title: Product service
    version: 1.0.0