server:
  port: 8083
spring:
  application:
    name: product-service
  profiles:
    active: @spring.profiles.active@

management: # Health check
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    root: INFO
    web: OFF
